{% extends 'base.html.twig' %}
{% block title %}Establecimiento {{ ctlEstablecimiento.nombre }}{% endblock %}
{% block body %}
<div class="row">
 <div class="col-xs-12">
  <div class="box">
   <div class="box-header with-border">
    <h4>Datos</h4>
    <div class="box-tools pull-right">
     <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Ocultar">
     <i class="fa fa-minus"></i></button>
     <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="" data-original-title="Salir">
     <i class="fa fa-times"></i></button>
    </div>
   </div>
   <div class="box-body" style="">
    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlEstablecimiento.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ ctlEstablecimiento.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ ctlEstablecimiento.telefono }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ ctlEstablecimiento.fax }}</td>
            </tr>
            <tr>
                <th>Registroschema</th>
                <td>{% if ctlEstablecimiento.registroSchema %}{{ ctlEstablecimiento.registroSchema|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Enableschema</th>
                <td>{{ ctlEstablecimiento.enableSchema }}</td>
            </tr>
        </tbody>
    </table>
   </div>
   <div class="box-footer" style="">
    <a href="{{ path('admin_personas_index') }}" class="btn btn-info"><span class="glyphicon glyphicon-list-alt" data-toggle="tooltip" title="" data-original-title="Listar"></span> Listar </a>
   </div>
  </div>
 </div>
</div>
{% endblock %}
