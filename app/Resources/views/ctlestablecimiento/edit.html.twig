{% extends 'base.html.twig' %}
{% block title %}Actualizar establecimiento{% endblock %}
{% block subtitle %}Actualizar cuadro basico para establecimiento{% endblock %}
{% block body %}
    <!-- Main content -->
    <section class="content">
      <div class="row">
        <div class="col-xs-12">
        
            {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editar" class="btn  btn-primary"/>
    {{ form_end(edit_form) }}
        
    <p>
     <a href="{{ path('establecimiento_index') }}" class="btn  btn-default">Regresar a la lista</a>
     <a href="{{ path('establecimiento_edit', { 'id': ctlEstablecimiento.id }) }}"  ></a>
     {{ form_start(delete_form) }}<input type="submit" value="Borrar" class="btn  btn-danger">{{ form_end(delete_form) }}
    </p>
        </div>
        <!-- /.col -->
      </div>
      <!-- /.row -->
    </section>    
{% endblock %}
